# Inventory Datasets

Use `config/dataset-config.ts` to choose which dataset the app loads.

- `tools`: original industrial warehouse demo data.
- `chocolates`: chocolate and candy inventory demo inspired by Hersheyland Mexico brand families.

JSON files cannot contain comments, so the original data is preserved in `data/tools` instead of being commented out.
